.DS_Store
*.log
app/.build/
app/build/
dist/

# never commit a boot profile — it holds the account name and password.
# Ashita v4 spells them .ini, v3 (Eden, ValhallaXI, FFEra) spells them .xml.
config/boot/*.ini
config/boot/*.xml
*.boot.xml
*.premetal
sweep-out/

# harness output
scripts/shots/
*.vkbak

# benchmark harness output (screenshots, per-run JSON, wine logs)
scripts/harness/results/
scripts/harness/shots/

# archived .app builds are binaries, not source
archive/app-builds/

# compiled harness helpers (menu-run.py builds game-window on demand)
scripts/harness/.build/

# local Docker test server logs
scripts/lsb-docker/log/
__pycache__/
